package data

fun main(){
    var benar = true
    var salah = false
    println("benar && salah (logic AND) = " + (benar && salah))
    println("benar || salah (logic OR) = " + (benar || salah))
    println("!benar (logic NOT) = " +(!benar))
}